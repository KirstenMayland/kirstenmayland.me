'use strict';

const e = React.createElement;

class Sudoku extends React.Component {

}

const domContainer = document.querySelector('#sudoku');
const root = ReactDOM.createRoot(domContainer);
root.render(e(Sudoku));